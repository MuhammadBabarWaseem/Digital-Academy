import ProfileComponent from '../components/Profile';
import Layout from '../layout/main';

const Profile = () => {
  return (
    <Layout>
      <ProfileComponent />
    </Layout>
  );
};

export default Profile;
